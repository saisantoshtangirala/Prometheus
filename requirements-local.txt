# ── Prometheus Local (Mac mini / Apple Silicon) ──────────────────────────
# Install with: pip install -r requirements-local.txt
# PyTorch is installed separately (see setup instructions below).
#
# PyTorch with MPS (Metal):
#   pip install torch torchvision torchaudio
#   (The standard pip wheel includes MPS since PyTorch 2.0)

# Core numerics
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.0.0

# Graph
networkx>=3.1

# Financial data
yfinance>=0.2.31
pandas-ta>=0.3.14b0

# Visualization (volcano + reports)
plotly>=5.17.0
matplotlib>=3.7.0

# AWS (pull/push checkpoints)
boto3>=1.34.0
awscli>=1.32.0

# Utilities
pydantic>=2.4.0
python-dotenv>=1.0.0
loguru>=0.7.0
rich>=13.6.0

# Optional: FinBERT sentiment (requires ~1.5 GB download)
# transformers>=4.35.0
